import { styled } from "styled-components";

const LogoutDiv = styled.div`
    
`;

const Logout = () => {
    return (
        <LogoutDiv>
            
        </LogoutDiv>
    );
}

export default Logout;