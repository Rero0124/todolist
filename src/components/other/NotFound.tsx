import { styled } from "styled-components";

const NotFoundDiv = styled.div`
    
`;

const NotFound = () => {
    return (
        <NotFoundDiv>
        </NotFoundDiv>
    );
}

export default NotFound;