import { styled } from "styled-components";

const RegisterDiv = styled.div`
    
`;

const Register = () => {
    return (
        <RegisterDiv>
        </RegisterDiv>
    );
}

export default Register;