import { styled } from "styled-components";

const LoginDiv = styled.div`
    
`;

const Login = () => {
    return (
        <LoginDiv>
            
        </LoginDiv>
    );
}

export default Login;